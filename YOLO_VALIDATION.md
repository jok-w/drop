# YOLO 接入验证（2026-09-22，历史基线）

以下统计来自当时的匀速预测与人工重建版本，不代表当前减速预测和自动重建版本的结果。

使用用户提供的惰性模型检测权重，SHA256：
`25fec41785b258d70bcb93214d07b1e55c365c1410797cb74263f6ad135037c0`。
权重类别为 `0: item`。没有重新训练。

## 已完成检查

- `python -m unittest discover -s tests -v`：21 项通过，其中 9 项覆盖检测跟踪，12 项为已有回归测试。
- 真实权重加载和 CPU 推理成功，默认 imgsz=1280、conf=0.25、nms-iou=0.5。
- 两种检测频率均完成 100 帧导出，回读 annotated.mp4、CSV 和 JSONL 均为 100 帧。
- 跳过检测的帧中观测 center 为空，Kalman 预测独立保存。
- 逐帧结果第 230、240、250、260、268、278 帧拼图经过目视抽查；没有逐帧人工真值。

## 运行统计（不是准确率）

| 输入与帧范围 | 检测间隔 | TRACKING | PREDICTED | LOST_PENDING | LOST |
|---|---:|---:|---:|---:|---:|
| 144729，第 230～329 帧 | 1 | 36 | 0 | 13 | 51 |
| 144729，第 230～329 帧 | 3 | 13 | 32 | 4 | 51 |
| 144704，第 90～149 帧 | 1 | 26 | 0 | 14 | 20 |

前两项最后一次观测均为第 266 帧。第 279 帧进入 LOST；缺失的真实轨迹不做填补。
输出分别位于 `outputs/yolo_baseline_144729`、`outputs/yolo_baseline_interval3`、`outputs/yolo_baseline_144704`。

## 方法选择与限制

先前的严格运动门限在运动变化时连续拒绝正确外观候选。因此新后端默认对唯一候选仅记录运动冲突，仍输出检测并更新 Kalman；不针对该片段放宽 gate 数值。
`--strict-motion-gate` 保留严格策略用于对照。多个候选无法经运动筛选唯一确定时拒绝更新。
默认策略可能接受唯一误检；两次观测初始化速度也不能保证目标身份。需要独立标注集评估误检、漏检、中心误差与身份切换。

当时 Kalman 使用匀速模型，快速变化时会滞后；当前版本已改用指数减速预测。不要用平滑度代替准确率。检测框中心不等于物理质心。
当前输出是相机画面像素轨迹，视频有相机运动时也会反映到轨迹中。未实现稳像、世界坐标或长期遮挡后的身份确认。
当时超时丢失后按 R 人工建立新轨迹段；当前版本会等待唯一同类检测自动建立新段。
